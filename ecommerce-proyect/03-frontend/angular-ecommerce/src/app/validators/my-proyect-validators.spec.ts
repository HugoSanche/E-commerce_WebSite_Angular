import { MyProyectValidators } from './my-proyect-validators';

describe('MyProyectValidators', () => {
  it('should create an instance', () => {
    expect(new MyProyectValidators()).toBeTruthy();
  });
});
